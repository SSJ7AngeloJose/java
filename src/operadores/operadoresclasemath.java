package operadores;

public class operadoresclasemath {

    public static void main(String[] args) {
        //el math funciona oslo con double
        //int raiz = (int)Math.sqrt(2);
        //es para sacar la raiz cuadrada de un numero

        // /*
        // double raiz = Math.sqrt(2);
        //        System.out.println("la raiz es :" + raiz);
        // */

        //la potenciacion
        // /*
        // double base = 5, exponente =  2;
        //        double resultado = Math.pow(base , exponente);
        //        System.out.println(resultado);double base = 5, exponente =  2;
        //        double resultado = Math.pow(base , exponente);
        //        System.out.println(resultado);
        // */

        //para redondear un nuemero
       /* double numero = 2.5;
        Long resultado = Math.round(numero);
        System.out.println(resultado);
          */

        /* Float numero = 3.5f;
        int resultado = Math.round(numero);
        System.out.println(resultado);*/

        //coimo sacar o obtener un numero aleatorio
        double numero = Math.random();
        System.out.println(numero);


    }
}
